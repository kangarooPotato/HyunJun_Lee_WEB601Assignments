import { FilterMovieTypePipe } from './filter-movie-type.pipe';

describe('FilterMovieTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMovieTypePipe();
    expect(pipe).toBeTruthy();
  });
});
